pipeline {
    agent any

    stages {
        stage ('Packaging Stage') {
                    sh 'mvn -B clean package'
        }
        stage ('Packaging Stage') {
                            sh 'mvn clean install'
                }
    }
}
